name: bewertungssystem_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # State Management - AKTUALISIERT
  riverpod: ^2.6.1
  flutter_riverpod: ^2.6.1
  
  # Navigation
  go_router: ^17.0.1
  
  # HTTP
  http: ^1.5.0
  
  # Freezed für immutable classes
  freezed: ^3.2.4
  freezed_annotation: ^3.1.0
  
  # JSON Serialization
  json_serializable: ^6.9.5
  
  # Charts - ERSETZT charts_flutter
  fl_chart: ^0.66.2
  
  # JWT Login
  flutter_secure_storage: ^10.0.0
  jwt_decoder: ^2.0.1
  
  # Shared Preferences (Standard Package - nicht platform-spezifisch!)
  shared_preferences: ^2.2.2
  
  # Path Provider (Standard Package)
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.5.4

flutter:
  uses-material-design: true